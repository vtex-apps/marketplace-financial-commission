type Query {
  """
  Retrieve all onboarded sellers
  """
  getSellers: Sellers @cacheControl(scope: PUBLIC, maxAge: LONG)
  """
  Retrieve all Invoices for a given seller
  """
  getInvoicesBySeller(seller: String!): Invoices
    @cacheControl(scope: PUBLIC, maxAge: LONG)
}
