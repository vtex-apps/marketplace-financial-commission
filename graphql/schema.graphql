type Query {
  """
  Retrieve all onboarded sellers
  """
  dashboard: Dashboards @cacheControl(scope: PUBLIC, maxAge: LONG)
  getSellers: Sellers @cacheControl(scope: PUBLIC, maxAge: LONG)
  stats: Stats @cacheControl(scope: PUBLIC, maxAge: LONG)
}
