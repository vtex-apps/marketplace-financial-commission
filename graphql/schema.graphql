type Query {
  """
  Retrieve all onboarded sellers
  """
  dashboard: DashboardsGRP @cacheControl(scope: PUBLIC, maxAge: LONG)
  getSellers: Sellers @cacheControl(scope: PUBLIC, maxAge: LONG)
  stats: StatsGRP @cacheControl(scope: PUBLIC, maxAge: LONG)
}
