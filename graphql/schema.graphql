type Query {
  """
  Retrieve all onboarded sellers
  """
  getSellers: Sellers @cacheControl(scope: PUBLIC, maxAge: LONG)
  searchSellersDashboard(
    dateStart: String!
    dateEnd: String!
    sellerId: String
    page: Int
    pageSize: Int
  ): ResultSearch @cacheControl(scope: PUBLIC, maxAge: LONG)
}
